# Codex CLI Example

1. Initialize memory with `memory-custodian init --with-codex`.
2. Add `adapters/codex/AGENTS.snippet.md` to `AGENTS.md` if it was not added automatically.
3. Keep full memory in `docs/memory/`.
