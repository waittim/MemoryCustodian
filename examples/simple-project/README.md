# Simple Project Example

Run from this repository after installing the CLI or with `PYTHONPATH=cli`:

```bash
memory-custodian init --project-root examples/simple-project
memory-custodian read --project-root examples/simple-project --task planning
```
