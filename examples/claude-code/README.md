# Claude Code Example

1. Initialize memory with `memory-custodian init --with-claude`.
2. Add `adapters/claude-code/CLAUDE.snippet.md` to `CLAUDE.md` if it was not added automatically.
3. Optionally copy command files into `.claude/commands/`.
